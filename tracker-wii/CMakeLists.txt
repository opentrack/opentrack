if(WIN32)
    include(opentrack-opencv)
    find_package(OpenCV QUIET)
    if(OpenCV_FOUND)
        foreach(k core imgproc)
            otr_install_lib("opencv_${k}" "${opentrack-libexec}")
        endforeach()
        add_subdirectory(wiiyourself)
        otr_module(tracker-wii)
        target_link_libraries(${self} PRIVATE opencv_imgproc opentrack-tracker-pt-base opentrack-wiiyourself bthprops)
        if(MINGW32)
            target_link_libraries(${self} PRIVATE hid)
        endif()
        target_include_directories(${self} PRIVATE "${CMAKE_SOURCE_DIR}/tracker-pt")
    endif()
endif()
