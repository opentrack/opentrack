otr_module(logic BIN)
target_link_libraries(${self} PRIVATE opentrack-spline)
if(NOT WIN32)
    target_link_libraries(${self} PRIVATE opentrack-qxt-mini)
else()
    target_link_libraries(${self} PRIVATE winmm)
    target_link_libraries(${self} PUBLIC opentrack-dinput)
endif()
